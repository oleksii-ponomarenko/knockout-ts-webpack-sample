const routes = {
	empty: '',
	home: '#/home',
	test: '#/test/:testId',
	passTest: '#/pass/test/:testId',
	passedTests: '#/test/:testId/assessments'
}

const routeTypes = {
	get: 'get'
}

export { routes };
export { routeTypes }